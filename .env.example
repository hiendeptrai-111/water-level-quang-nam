# ============ Backend ============
PORT=4000
APP_VERSION=1.0.0

# CORS allowed origins (comma-separated). Use specific origin in production.
CORS_ORIGIN=http://localhost:5173,http://localhost

# ============ MySQL ============
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=water_level_db

# ============ Auth ============
JWT_SECRET=change-this-to-a-long-random-string-in-prod

# ============ Optional ============
# SCRAPE_INTERVAL_MS=600000
